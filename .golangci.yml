version: "2"

linters:
  default: standard

issues:
  exclude-rules:
    # httptest handlers: unchecked fmt.Fprint to ResponseWriter is idiomatic in tests.
    - path: '.*_test\.go'
      linters:
        - errcheck
